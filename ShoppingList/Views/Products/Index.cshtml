@model IEnumerable<ShoppingListDAL.Models.Products>

@{
    ViewData["Title"] = "Index";
}

<h1>Games</h1>
<div class="container">
    @for (int i = 0; i < Model.Count(); i++)
    {
        <div class="w-25 m-3 d-inline-block">
            <div class="card text-center">
                <h5 class="card-title">@Model.ElementAt(i).Name</h5>
                <p class="card-text">@Model.ElementAt(i).Price</p>
                @Html.ActionLink("Add to Cart", "AddToCart", "Products",
            routeValues: new { productId = Model.ElementAt(i).Id },
            htmlAttributes: new { @class = "btn btn-primary" })
            </div>
        </div>
    }
</div>



@*<table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Price)
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @item.Name
                    </td>
                    <td>
                        @item.Price.ToString("c")
                    </td>
                    <td>
                        @Html.ActionLink("Add to Cart", "AddToCart", "Products", routeValues: new { productId = item.Id }, htmlAttributes: new { @class = "btn btn-primary" })
                    </td>
                </tr>
            }
        </tbody>
    </table>*@
